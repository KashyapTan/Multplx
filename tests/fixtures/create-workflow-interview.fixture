Purpose: turn a support issue into a verified local fix.
Stage 1: the human and orchestrator agree on a reproduction; approval required; output is a reproduction note.
Stage 2: a fresh implementation sub-agent fixes it from that note; auto-advance after a local commit.
Stage 3: run a deterministic focused test command; auto-advance on exit zero.
